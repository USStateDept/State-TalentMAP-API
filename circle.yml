machine:
  python:
    version: 3.6.0
test:
  pre:
    - mkdir -p $CIRCLE_ARTIFACTS/pytest-cov/
  override:
    - pytest --cov=talentmap_api --cov-report xml:$CIRCLE_ARTIFACTS/pytest-cov/coverage.xml --cov-config .coveragerc
